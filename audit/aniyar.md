# Независимый аудит жюри — Career Quest

Автор: Aniyar Baibossyn (аудит с Codex). В этой ветке опубликован только отчёт. Указанные ниже JSON/CSV — имена локальных доказательств из архива jury-audit.zip, переданного в чате; они не добавлены в эту ветку.

23 сентября 2026. Проверен локальный код SHA `dfa1e39fc1efc1f2f5b194875e80fc3e353aee5d` на порту 8010, модель `gpt-6-luna`. Это фактически прежняя рабочая копия, а не новый чистый клон. Запуск `python run.py --port 8010` прошёл; для tools изменён локальный .env и сервер перезапущен с `--skip-build`. Ключ не выводился и не включён в материалы. Изменений кода, коммитов и push в рамках аудита нет. Значение ключа, ошибочно попавшее в отслеживаемый example, убрано оттуда и сохранено в локальном .env.

По стоп-инструкции дальнейшие проверки остановлены. Ниже только собственные наблюдения. Русский интерфейс и известная ошибка склонения не считаются находками.

## P0

**Не воспроизведены:** запуск работает; загрузка трёх новых профилей через `/api/dataset/upload` успешна; рекомендации возвращаются; выдуманных event_id или ошибок в структурированных gains/готовности не найдено. Это не означает отсутствия всех возможных P0.

## P1-1 — single: текст противоречит последовательному плану

Новый профиль **J902**, `source=llm`, `latency_ms=6599`:

- План: EV_006 → EV_007 → EV_010.
- EV_006 повышает System Design 2→3.
- Объяснение EV_007: «круг поможет поднять его с 2 до 3».
- Фактический `gains` EV_007: `SK_SYSTEM_DESIGN`, `before=3`, `after=4`.

**FAIL по согласованности объяснения**, хотя структурированный план корректен. Подтверждение: single/J902.json (`single/J902.json`).

Аналогично **E0052, kk**, `source=llm`, `latency_ms=7166`: после EV_031 Public Speaking уже 2, но объяснение EV_036 говорит «бұл дағдыны 1→2 көтереді»; `gains` содержит 2→3. single/E0052.json (`single/E0052.json`).

В tools для J902 ошибка не повторилась: текст явно говорит «После первого шага ... с 3 до 4», совпадая с gains. tools/J902.json (`tools/J902.json`). Для E0052 tools вернул fallback, поэтому успешную проверку именно LLM-объяснения kk в tools не заявляю.

Практическое исправление для владельца AI: формировать числовые утверждения из окончательного результата simulate_plan либо передавать модели именно последовательные before/after выбранного плана; при противоречии использовать объяснение движка. Это рекомендация, код в аудите не изменялся.

## P1-2 — tools: реальные таймауты вместо LLM-ответа

При новых запросах после перезапуска/импорта:

| Профиль | source | latency_ms | HTTP, мс | Результат |
|---|---|---:|---:|---|
| E0072 ru | fallback | 9000 | 9005 | FAIL требования реального LLM; ответ приложения своевременный |
| E0052 kk | fallback | 9004 | 9009 | FAIL требования реального LLM; ответ приложения своевременный |

Оба trace содержат: `LLM недоступен (TimeoutError) — объяснение сформировал движок`.
Источники: tools/E0072.json (`tools/E0072.json`), tools/E0052.json (`tools/E0052.json`).

Также первый штатный tools-eval дал FAIL на T001/T002 из-за fallback, HTTP 8.994/9.025 с. Повторные обращения получились успешными: 7388/8919 мс. Это не отменяет первых сбоев. Первый eval (`eval-tools.json`), повторные ответы (`tools/summary.json`). Поле latency_ms первых двух fallback штатный eval не сохранил; не подменяю его HTTP-временем.

В прерванном контрольном eval T002 снова вернул fallback (`latency_ms=9007`, HTTP 9.013 с); это было в консольном выводе, полный файл контрольного прогона не записан из-за остановки. Контрольный прогон не засчитан как завершённый.

Ни один завершённый измеренный запрос не превысил 10 с. Проблема — нестабильное получение LLM-результата в двухраундовом режиме. Маленькая выборка не позволяет оценивать процент надёжности сервиса. Перед сдачей нужен запас времени внутри двух раундов, а не увеличение общего лимита выше ТЗ.

## Три новых профиля жюри

Все профили созданы независимо, не скопированы из T001–T005; ID J901–J903. Загрузка обоих файлов вместе — без предупреждений.

| Профиль | Ловушки | single | tools | Оценка |
|---|---|---|---|---|
| J901 | Data Analyst Middle → Product Manager Middle; низкий Public Speaking; 3 no_show/declined по теме; remote и офлайн | llm, 5635 мс; EV_027→EV_026→EV_021 | llm, 6421 мс; тот же план | PASS: выбраны мероприятия целевой роли, несколько разрывов, критичность, история и формат учтены |
| J902 | Backend Middle → Senior; критичные System/API Design против Public Speaking=0; 3 срыва клуба; remote | llm, 6599 мс; EV_006→EV_007→EV_010 | llm, 8365 мс; тот же план | single FAIL текста (P1-1); tools PASS |
| J903 | Statistics=0 блокирует A/B workshop, Python=1 блокирует ML; 3 срыва офлайн/выступлений; remote | llm, 4459 мс; EV_020→EV_021→EV_012 | llm, 7310 мс; тот же план | PASS: статистика первым шагом открывает пререквизит второго; модель не предлагает закрытый курс первым |

У всех рекомендованных шагов минимум 3 разных вида факторов (фактически 4–7). Проверены role/grade/career_goal, mandatory, история завершений, сессии, пререквизиты в порядке плана, title/format/duration, gains с max_level, сумма score и готовность после каждого шага. Сверка выполнена независимым скриптом с исходными `events.json` и `skills.json`, без импорта engine. **Все структурированные проверки PASS**; семантическая ошибка текста single выявлена отдельно.

J901: офлайн EV_026 допустим как мягкий компромисс — объяснение прямо упоминает remote/неудобный формат и прошлые срывы; запрета на весь офлайн в ТЗ нет. J903: EV_020 поднимает Statistics 0→1, только затем допустим EV_021 (требование ≥1).

## Языки и согласованность

| Запрос | single | tools | Оценка текста |
|---|---|---|---|
| E0072 ru | llm, 5929 мс | fallback, 9000 мс | Русский корректный, факты и приросты совпадают. tools не засчитан как реальный LLM |
| E0052 kk | llm, 7166 мс | fallback, 9004 мс | Казахский понятный, но single содержит числовое противоречие EV_036; FAIL согласованности |
| E0028 en | llm, 4288 мс | llm, 6090 мс | Английский корректный; EV_037, Mentoring 2→3; tools readiness 73→75 соответствует расчёту |

Казахский: присутствуют нормальные конструкции «өз қарқыныңызбен», «ерікті белсенділік», «қажетті деңгейі». Есть стилистически неровные формулировки: «ал сіз 2→3 көтересіз» лучше заменить на «дағды деңгейін 2-ден 3-ке көтересіз»; «бұл тақырыпта үзіліс болмаған» менее точно передаёт отсутствие срывов участия. Названия навыков из каталога оставлены английскими; это не смена языка ответа. Главный дефект kk — неверные before/after, а не сам выбор языка. Это экспертная языковая оценка ответа, не отдельная проверка носителем.

## Справка по стандартному eval (новые запуски прекращены)

single: T001 llm 9632 мс; T002 llm 6047; T003 llm 5086; T004 llm 6936 — штатный eval PASS. T005 fallback, пустой план — ожидаемый PASS, LLM не нужен. Эти замеры отличаются от ранее сообщённых командой 5.2–7.2 с; они не отменяют ни тот, ни этот прогон. Ответ T001 почти исчерпал лимит 10 с.

tools: первый eval T001/T002 FAIL fallback, T003 llm 7363 мс PASS, T004 llm 8619 PASS, T005 ожидаемый fallback PASS. Сохранённые успешные повторы T001/T002 отдельно помечены выше; из них нельзя делать вывод «первый прогон 5/5».

`latency_ms` из API — длительность генерации, а не время повторного попадания в кэш. T-профили повторно читались для сохранения raw JSON: HTTP-время кэш-хита не выдаётся за ускорение LLM. J/E-запросы выполнялись после импорта новых профилей, без прежнего кэша.

## Файлы

- employees.json (`employees.json`) — 3 профиля.
- activity_history.csv (`activity_history.csv`) — 10 записей истории.
- structured-checks.json (`structured-checks.json`) — независимая числовая проверка 22 сохранённых ответов.
- single/summary.json (`single/summary.json`), tools/summary.json (`tools/summary.json`) — source и latency_ms.
- Полные ответы с профилями и trace: папки single/ и tools/.

**Вердикт:** новые профили показывают многофакторный выбор, корректную смену роли и работу пререквизитов. До строгого PASS объяснимости мешают P1-1; до стабильной демонстрации tools — P1-2. Запуск и структурированная доменная логика проверенных случаев работоспособны.
